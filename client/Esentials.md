# Skywatch

## Scope
- Focus exclusively on the `client/` directory for any modifications or feature implementations.
- Do not modify files in the `api/` or `server/` directories.

## Tech Stack
- Frontend: Angular 17 (Standalone components, Signals, New control flow).
- Backend (Reference only): PHP API and Node.js server.
