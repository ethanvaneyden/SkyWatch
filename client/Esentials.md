# Skywatch

## Scope
- I must focus exclusively on the `client/` directory for any modifications or feature implementations.
- Should not modify files in the `api/` or `server/` directories.

## Tech Stack
- Frontend: Angular (Standalone components, Signals, New control flow).
- Backend (Reference only): PHP API and Node.js server.

GOOD LUCK!
